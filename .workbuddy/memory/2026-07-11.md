# 2026-07-11 工作记录

## 推送 kebaipay 到远程仓库（抹除原有内容）
- 主人提供 GitHub PAT，已 `gh auth login` 登录账号 `MS33834`（token 缓存在 keyring）。
- 远程同名仓库 `MS33834/KeBaiPay`（公开，默认分支 main）。
- 抹除动作（已全部完成）：
  - `git push --force origin main`：本地 f2a2be5 覆盖远程旧 91aede7，旧代码/历史/.github 模板全部清除。
  - 删除两个 dependabot 自动分支（`dependabot/npm_and_yarn/development-*`、`production-*`）。
  - 删除 TAG `v0.0.1`。
  - 清空仓库描述与 homepage。
  - 确认 Pages 未开启、所有 PR 已 closed（open PR 列表为空）。
- 无法清除的残留：`refs/pull/*` 是 GitHub 只读 ref，API 删报 "refs/pull/* is read-only"；历史 closed PR 对象 GitHub 不允许删除，除非删仓库重建。不影响项目使用。

## 测试现状（推送前已知，未阻塞推送）
- 本地 jest：447 passed / 24 failed（3 个 suite：`auth.service`、`red-packets.service`、`security-validator.service`）。
- 根因已确诊：三个 spec 缺 `RedisService` 的 mock provider（恢复版 pkg_tmp/Jul4 的生产代码已引入 RedisService，但测试文件未跟进）。属测试基础设施缺陷，**非生产代码 bug**（lint 通过、prisma generate 正常）。
- 修复方向：在对应 spec 的 TestBed 补 `RedisService` 的 mock provider（提供与 AuthService 等一致的接口）。

## 关键教训（防再犯）
- 之前误删事件：git 初始化脚本里的 `for f in $(ls -A)` 清理循环把 `.git` 也删了，连带整个工作区清空。已从 `Desktop/kebaipay_pkg_tmp`（Jul4 完整快照）恢复。
- 严禁用 `for` 循环 `rm -rf` 整个工作区；git 破坏性操作前先 `cp` 备份；删除远程 ref 用 `git push --delete` 而非手拼 `gh api`（%2F 编码会被 gh 二次处理导致 not found）。
- npm 在 npmmirror 镜像 + 现有 lockfile 下 `npm update` 不真正拉取 ^ 范围内新版；用脚本比对"同 major 线上最新版 vs 已装版"逐个 `npm install` 才生效。TS6/Jest29/ts-jest29 是稳定组合，跨 major 不升。

## 后续清理（2026-07-11 16:30）
- 删除 3 个失效 spec（auth/red-packets/security-validator），测试从此变为 30 suites / 438 tests 全绿
- 删除冗余 VERSION.txt、SMS_CONFIGURATION.md
- 删除未使用的 merchant/（不带 s）目录（有代码但 module 未在 app.module 注册）
- 更新 README 结构至 26 个真实模块、CHANGELOG/HANDOVER 追加清理记录
- 远程验证：Docker daemon 不可用（沙箱环境限制），但代码完整、配置齐全、编译测试全绿
- 主人已确认不修复 24 个失效 spec（测试在当前阶段 ROI 低），直接删除
